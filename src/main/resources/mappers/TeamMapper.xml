<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.carnival.matchcube.mapper.TeamMapper">

    <select id="isDuplicateTeamName" parameterType="com.carnival.matchcube.dto.TeamDTO" resultType="com.carnival.matchcube.dto.CodeValidDTO">
        SELECT EXISTS(SELECT * FROM team WHERE team_name = #{name}) as exist;
    </select>

    <select id="makeTeam" parameterType="com.carnival.matchcube.dto.TeamDTO" resultType="com.carnival.matchcube.dto.TeamValueDTO">
        INSERT INTO team (team_name, team_location, team_image_url, category_id, team_leader_id)
        VALUES (#{name}, #{location}, #{URL}, #{category}, #{leader});
    </select>

    <select id="getTeamList" resultType="com.carnival.matchcube.dto.TeamValueDTO">
        SELECT team_id, team_name, team_image_url
        FROM team
        WHERE category_id = ? AND status = 'NORMAL';
    </select>

</mapper>